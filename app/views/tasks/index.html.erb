<h1>タスク一覧</h1>
  
<ul>
  <% if @tasks.any? %>
    <% @tasks.each do |task| %>
      <li><%= link_to task.id, task %> : <%= task.status %> &gt; <%= task.content %> </li>
    <% end %>
  <% end %>
   
</ul> 

<%= link_to '新規タスク作成', new_task_path %>